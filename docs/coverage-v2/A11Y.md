# Coverage v2 — Lighthouse Erişilebilirlik Raporu

**Tarih:** 2026-07-03  
**Sayfa:** `/coverage/crwv-a11y-temp` (production build üzerinden)  
**Araç:** Lighthouse 13.4.0 (desktop preset, yalnızca Accessibility kategorisi)  
**URL:** `http://localhost:3456/coverage/crwv-a11y-temp`

## Sonuç

| Metrik | Değer | Hedef |
|---|---|---|
| Erişilebilirlik skoru | **96 / 100** | ≥ 95 ✅ |

## Denetim Özeti

Tüm otomatik denetimler başarılı geçti:

- ✅ `<html>` elemanında geçerli `lang` özelliği.
- ✅ Sayfa başlığı (`<title>`) mevcut.
- ✅ Ana landmark (`<main>`) mevcut.
- ✅ Butonlar, bağlantılar ve ARIA kontrolleri erişilebilir isimlere sahip.
- ✅ Görüntü elemanları `alt` metnine sahip.
- ✅ Renk kontrastı yeterli (metin ve arayüz elemanları).
- ✅ Başlık hiyerarşisi sıralı.
- ✅ Form elemanları etiketlere sahip.
- ✅ Doküman yakınlaştırmaya izin veriyor (`maximum-scale=5`).
- ✅ SVG görsellerde `<title>` / `<desc>` alternatifleri korunuyor.

## Düzeltilen Bulgular

Faz 5 kalite kontrolü sırasında aşağıdaki iki bulgu giderildi:

1. **Meta viewport:** `maximum-scale=1` değeri `maximum-scale=5` olarak güncellendi (`client/index.html`).
2. **Kontrast:** Üst navigasyondaki pasif workspace butonlarının metin rengi `text-muted-foreground` yerine `text-foreground/80` olarak ayarlandı (`client/src/App.tsx`).

## Sınırlamalar

- Bu rapor otomatik Lighthouse denetimine dayanır; manuel ekran okuyucu ve klavye navigasyonu testleri ayrıca önerilir.
- Rapor, geçici bir CRWV coverage raporu (`reports/coverage/crwv-a11y-temp.md`) üzerinde üretilmiştir; canlı raporlarda içerik değişiklikleri sonucu tekrar kontrol edilmelidir.

## Ham Veri

- `docs/coverage-v2/lighthouse-a11y.json`
